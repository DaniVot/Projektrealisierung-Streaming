package com.example.webcamstreaming.ui.components

/**
 * Status used for UI indicators (cards/detailed view) about the current HLS stream.
 */
enum class StreamStatus {
    STREAMING,
    BUFFERING,
    ERROR
}

